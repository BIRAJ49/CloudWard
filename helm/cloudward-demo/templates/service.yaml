apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudward-demo.fullname" . }}
  labels:
    {{- include "cloudward-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cloudward-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
