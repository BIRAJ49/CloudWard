apiVersion: v1
kind: Pod
metadata:
  name: host-network-demo
  namespace: cloudward-staging
spec:
  hostNetwork: true
  containers:
    - name: app
      image: registry.k8s.io/pause:3.10
      securityContext:
        privileged: false
        capabilities:
          drop: ["ALL"]
