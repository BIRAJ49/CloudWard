apiVersion: v1
kind: Pod
metadata:
  name: privileged-demo
  namespace: cloudward-staging
spec:
  containers:
    - name: app
      image: registry.k8s.io/pause:3.10
      securityContext:
        privileged: true
        capabilities:
          drop: ["ALL"]
