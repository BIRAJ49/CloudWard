apiVersion: v1
kind: Pod
metadata:
  name: production-demo-target
  namespace: cloudward-production
  labels:
    cloudward.io/demo-target: "true"
spec:
  containers:
    - name: app
      image: registry.k8s.io/pause:3.10
      securityContext:
        privileged: false
        capabilities:
          drop: ["ALL"]
