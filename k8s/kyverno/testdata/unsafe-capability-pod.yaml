apiVersion: v1
kind: Pod
metadata:
  name: unsafe-capability-demo
  namespace: cloudward-staging
spec:
  containers:
    - name: app
      image: registry.k8s.io/pause:3.10
      securityContext:
        privileged: false
        capabilities:
          add: ["NET_ADMIN"]
          drop: ["CHOWN"]
